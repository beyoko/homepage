import { Section, Text, Box, Container, Heading } from "@chakra-ui/react";

const Home = () => {
  return (
    <Container>
      <Heading as="h3" variant="section-title">
        work
      </Heading>
      <Heading as="h3" variant="section-title">
        think
      </Heading>
      <Text>test</Text>
    </Container>
  );
};

export default Home;
